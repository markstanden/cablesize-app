export enum CableTableError {
   InvalidFormData = "Invalid Data Within Form Submission",
   InvalidRefMethodForCableType = "Invalid Reference Method For Cable Type",
   InvalidStore = "Invalid Cable Table Store",
   OutOfApplicationScope = "Required value is outside the scope of the BS7671 Tables, and is therefore outside of the scope of the application.",
}
